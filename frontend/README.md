# Frontend

React + TypeScript application bootstrapped for Vite. Components are placeholders until real UI lands in Phase 2.

## Commands
- `npm run dev`
- `npm run build`
- `npm run test`

Follow `docs/development-setup.md` for environment details.
